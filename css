header {
    align-items: left;
    display: grid;
    grid-template-columns: 110px auto;
    padding:1em;
    backround-color:yellow;

 footer {
  padding: 1em;
  text-align:center;
  backround-color:yellow;
}

 body {
  font-family:sans-serif;
  font-size:1.em; 
 }